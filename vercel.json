{
  "version": 2,
  "buildCommand": "chmod +x node_modules/.bin/* 2>/dev/null; npm run build",
  "installCommand": "npm install --no-audit --no-fund --no-bin-links",
  "framework": "vite",
  "outputDirectory": "dist",
  "engines": {
    "node": "18.x"
  }
}
